version: "2"
run:
  modules-download-mode: readonly
linters:
  enable:
    - bodyclose
    - contextcheck
    - misspell
    - nilerr
  settings:
    errcheck:
      check-blank: false
      exclude-functions:
        - (io.Closer).Close
        - (net.Listener).Close
    govet:
      disable:
        - fieldalignment
      enable-all: true
    staticcheck:
      checks:
        - all
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - bodyclose
          - contextcheck
          - errcheck
        path: _test\.go
      - linters:
          - errcheck
          - govet
          - unused
        path: \.pb\.go$
      - path: config/
        text: hardcoded
      # 排除 contextcheck 中因函数签名固定而无法传递 context 的告警
      - linters:
          - contextcheck
        text: "should pass the context parameter"
      - linters:
          - contextcheck
        text: "Non-inherited new context"
      # 测试文件中结构体字面量写入未读取字段属于正常做法，排除该告警
      - linters:
          - govet
        text: "unused write to field"
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 50
  max-same-issues: 10
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
